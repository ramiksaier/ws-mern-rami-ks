import React from "react";
import { Spinner } from "react-bootstrap";
const Spiner = () => {
  return (
    <div>
      <Spinner className="spi" animation="border" variant="primary" />
    </div>
  );
};

export default Spiner;
